'use client';

import React from 'react';
import { GameConfig } from '@/lib/games-data';

interface SideMarginsBackgroundProps {
  game?: GameConfig;
}

export function SideMarginsBackground({ game }: SideMarginsBackgroundProps) {
  // Temporarily disabled as requested
  return null;
}
