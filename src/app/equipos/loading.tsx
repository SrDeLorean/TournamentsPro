import { RouteLoading } from '@/components/ui/route-loading';

export default function EquiposLoading() {
  return <RouteLoading label="Cargando directorio de equipos" variant="directory" cards={9} />;
}
