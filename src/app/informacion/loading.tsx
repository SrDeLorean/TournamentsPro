import { RouteLoading } from '@/components/ui/route-loading';

export default function InformacionLoading() {
  return <RouteLoading label="Cargando información de la plataforma" variant="directory" cards={4} />;
}
